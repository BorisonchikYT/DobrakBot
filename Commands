`/user`, `/server`, `/bio`, `/bot`, `/color`, `/say`.
`/kick`, `/ban`, `/unban`, `/mute`, `/unmute`, `/warn`, `/unwarn`, `/support`, `/slowmode`, `/yverify`, `/verify`, `/text-welcome`, `/text-verify`, `/start-welcome`, `/anti-ssilka`, `/reset w`, `/reset v`, `/ticket-menu`, `/ticket-config`, `/ticket-stats`, `/clear-warns`, `/list-warn`.
`/bal`, `/give-money`, `/job`, `/crime`, `/del-bank`, `/add-bank`, `/reset e`, `/reset m`, `/hack`, `/miner`, `/pay`, `/casino`, `/work`, `/vacancy`, `/get-a-job`, `/shop-besinees`, `/buy-besinees`, `/shop-auto`, `/poauto`, `/buy-auto`, `/duel`, `/race`, `/promo`, `/create-promo`, `/delete-promo`, `/top`, `/pomauto`, `/buy-home`, `/shop-homes`, `/give-bank`, `/shop-slot`, `/add-role`, `/del-role`, `/buy-slot`, `/sell`, `/play`.
`/stream`, `/video`, `/akk`, `/button`.
`/hit`, `/cry`, `/kiss`, `/sleep`, `/escape`, `/cuddle`, `/anecdote`, `/bonus`, `/ben`, `/ball`, `/case`, `/box`, `/rank`.
Premium Tier 1 — `/pbonus`, `/clear-crash`, `/yprole`, `/pwork`, `/pvideo 1`, `/pstream 1`.
Premium Tier 2 — `/anti-crash`, `/pvideo 2`, `/pstream 2`.
